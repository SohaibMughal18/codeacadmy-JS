const kelvin = 293; // kelvin is 293
 const celsius = kelvin -273; // celcius
 let fahrenheight = (celsius * (9 /5) +32); // formula to calculate fahrenheight
fahrenheight = Math.floor(fahrenheight); // for geting output in numbers
console.log(` The temprature is ${fahrenheight} degrees fahrenheight `);
let newton = celsius * (33/100);
newton = Math.floor( newton);
console.log(` The temprature is ${newton} degrees newton `);

